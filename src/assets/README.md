# Assets

Source assets are organized under `public/assets` so they can be served by Vite without bundling. This directory is reserved for importable asset adapters if a feature later needs module-based imports.
